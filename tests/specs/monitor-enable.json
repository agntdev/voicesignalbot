[
  {
    "name": "Enable Chat with no chats shows empty state",
    "steps": [
      {
        "send": { "callback": "monitor:enable" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "Toggle monitoring for selected chat\n\nNo chats to enable. Add a chat first via 📡 Monitor."
            }
          }
        ]
      }
    ]
  }
]
